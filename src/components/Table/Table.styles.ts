import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tableContainer: { flex: 1, padding: 10 },
  tableContentContainer: { flex: 1, flexDirection: 'row' },
  nameColumn: { borderRightWidth: 1 },
  detailsColumn: { flex: 1 },
});
