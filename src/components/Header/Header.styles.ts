import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    flexDirection: 'row',
    backgroundColor: 'red',
    zIndex: 1,
    borderBottomWidth: 1,
  },
  headerScrollView: { borderLeftWidth: 1 },
});
